
from django.urls import path

urlpatterns = [
    # path('contests/', RegisterView.as_view(), name='contests'), #send constests with details like if user has appplied/given exam or not, sorted  by date
    # path('contests/<uuid:contest_uuid>/', RegisterView.as_view(), name='contests'),
    # path('contests/<uuid:contest_uuid>/problems/',)
    # path('contests/<uuid:contest_uuid>/submissions/')
    # path('contests/<uuid:contest_uuid>/standings') #depending on the score in contestproceess
    # path('contests/<uuid:contest_uuid>/apply') #create a  conteestprocess with status=Pending
    # path()
    #   
]
